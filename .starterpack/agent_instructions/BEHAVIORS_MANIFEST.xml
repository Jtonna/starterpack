<behaviors-manifest>

  <purpose>
    Index of all available behaviors. The orchestrator reads this at session start to know
    what behaviors exist. Load individual behavior files on demand when composing agent
    instructions — do not read all behavior files upfront.
  </purpose>

  <behavior name="git-conventions" file="behaviors/git-conventions.xml">
    Branching strategies (trunk-based, feature branching), branch naming conventions,
    commit message format, and push discipline.
  </behavior>

  <behavior name="escalation" file="behaviors/escalation.xml">
    Failure protocol for all agent types. Retry rules, structured failure reports,
    model-tier escalation chains (light → worker → reasoning → human).
  </behavior>

  <behavior name="scope-enforcement" file="behaviors/scope-enforcement.xml">
    File scope limits and out-of-scope discovery reporting. Prevents scope creep
    at both orchestrator and agent level. Agents only modify assigned files.
  </behavior>

  <behavior name="sub-task-tracking" file="behaviors/sub-task-tracking.xml">
    Post and update sub-task progress as structured comments on GitHub issues.
    Timeline of progress preserved as sequential comments.
  </behavior>

  <behavior name="documentation-structure" file="behaviors/documentation-structure.xml">
    Documentation model for single-project and monorepo layouts. Defines where docs
    live (README → INDEX → ARCHITECTURE → service docs) and organizing principles.
  </behavior>

  <behavior name="pr-template" file="behaviors/pr-template.xml">
    Pull request body template: summary, changes table, AI suggested testing plan,
    ticket link. Used by the PR drafter to compose the PR description.
  </behavior>

  <behavior name="human-gate" file="behaviors/human-gate.xml">
    Defines what a human gate is, the approval protocol (approve/reject/abort),
    and lifecycle-specific gate contexts defining what to present at each gate.
  </behavior>

  <behavior name="response-format" file="behaviors/response-format.xml">
    Standard response format for all agents. Every message prefixed with
    LIFECYCLE: {name} // {message} so humans can track active phases.
  </behavior>

  <behavior name="create-behavior" file="behaviors/create-behavior.xml">
    Rules, constraints, and XML skeleton template for writing behavior files.
    Covers self-containment, naming, structural complexity, rule formatting,
    and how to register new behaviors in the manifest.
  </behavior>

  <behavior name="create-lifecycle" file="behaviors/create-lifecycle.xml">
    Rules, constraints, and XML skeleton template for writing lifecycle files.
    Covers behavior referencing, phase structure, actor specificity, flow diagrams,
    naming conventions, and how to register new lifecycles in the manifest.
  </behavior>

  <behavior name="ci-gate" file="behaviors/ci-gate.xml">
    Protocol for waiting on CI/CD status checks after a pull request is created.
    Covers how to poll for check results, interpret outcomes, and structure reports
    for passed, failed, and no-checks-configured scenarios.
  </behavior>

  <behavior name="autonomous-nightly" file="behaviors/autonomous-nightly.xml">
    How to assess whether a ticket has enough information for autonomous execution,
    how to request clarification when it does not, and PR merge rules for autonomous work.
  </behavior>

</behaviors-manifest>
